package utility;

public enum Behavior {
	///*
	crush, grasp, high_velocity_shake, hold, 
	lift_slow, low_drop, poke, push, shake, tap, look
	/*/
	crush, grasp, hold, lift_slow, low_drop, poke, push, shake, tap, all
	//*/
}